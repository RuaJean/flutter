import 'package:flutter/material.dart';

class FavoritesPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Aquí solo retornamos un widget que represente el contenido
    return Center(
      child: Text('Aquí aparecerán tus películas favoritas.'),
    );
  }
}
